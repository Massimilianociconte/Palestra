const MAX_TIMER_DURATION_S=1200,MAX_REST_MS=12e5;function clampEndTime(e){const t=Date.now();if(e<t)return t;const n=t+12e5;return e>n?n:e}function clearTimer(){self.timerInterval&&(clearInterval(self.timerInterval),self.timerInterval=null)}self.onmessage=function(e){const t=e.data||{};if("start"===t.action){let e=Number(t.duration);if(!Number.isFinite(e)||e<=0)return;e=Math.min(e,1200),clearTimer(),self.endTime=Date.now()+1e3*e,self.timerInterval=setInterval(()=>{const e=Date.now(),t=Math.ceil((self.endTime-e)/1e3);t<=0?(clearTimer(),self.postMessage({action:"complete"})):self.postMessage({action:"tick",remaining:t})},250)}else if("stop"===t.action)clearTimer(),self.endTime=0;else if("adjust"===t.action&&self.timerInterval&&self.endTime){let e=Number(t.seconds);if(!Number.isFinite(e))return;e=Math.max(-1200,Math.min(1200,e)),self.endTime=clampEndTime(self.endTime+1e3*e);const n=Math.max(0,Math.ceil((self.endTime-Date.now())/1e3));self.postMessage({action:"tick",remaining:n})}};
//# sourceMappingURL=timer.worker.js.map