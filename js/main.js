document.addEventListener("DOMContentLoaded",()=>{const mobileToggle=document.getElementById("mobileToggle"),navLinks=document.getElementById("navLinks");mobileToggle&&navLinks&&mobileToggle.addEventListener("click",()=>{navLinks.classList.toggle("active"),mobileToggle.textContent=navLinks.classList.contains("active")?"✕":"☰"}),console.log("GYMBRO initialized."),"serviceWorker"in navigator&&navigator.serviceWorker.getRegistrations().then(registrations=>{if(registrations.length>0){console.log("Found existing Service Workers. Unregistering to ensure stability...");for(let registration of registrations)registration.unregister()}}),document.addEventListener("click",e=>{const toggle=e.target.closest(".password-toggle");if(!toggle)return;e.preventDefault();const wrapper=toggle.closest(".password-wrapper"),input=wrapper?wrapper.querySelector("input"):null;if(input){const isPassword="password"===input.type;input.type=isPassword?"text":"password",toggle.textContent=isPassword?"🔒":"👁️",toggle.title=isPassword?"Nascondi":"Mostra"}})});