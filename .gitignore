node_modules/
.backup/
.DS_Store
Thumbs.db
*.log
.vscode/

# Firebase Functions
functions/.env
functions/.env.local
.runtimeconfig.json

# Secrets
*.secret
.env
.env.local

# Firebase Config — NOT gitignored for web (needed by GitHub Pages)
# Firebase web API keys are public by design; security is enforced via Firestore Security Rules.
# Only native app config remains gitignored (uses different keys).
myapp-native/js/firebase-config.js

# Android build output (contains copies of source files)
myapp-native/android/app/src/main/assets/public/
