node_modules/
.DS_Store
Thumbs.db
*.log
.vscode/

# Firebase Functions
functions/.env
functions/.env.local
.runtimeconfig.json

# Secrets
*.secret
.env
.env.local

# Firebase Config (contiene chiavi API)
# NOTE: dist/js/firebase-config.js is NOT gitignored — it must be deployed to GitHub Pages.
# Firebase web API keys are public by design; security is enforced via Firestore Security Rules.
js/firebase-config.js
myapp-native/js/firebase-config.js

# Android build output (contains copies of source files)
myapp-native/android/app/src/main/assets/public/
